# TCP — Controller is TCP Server, Simulator is TCP Client
controller.host=localhost
controller.command.port=5000
controller.event.port=5001

# StatusBroadcaster sends STAT every N milliseconds
stat.interval.ms=5000
